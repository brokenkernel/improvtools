# I often work offline. This is a another TODO instead of github
- move jquery and other such things to local static so it works offline
- add widget support for suggestions
- I am misusing viewmodels for many aspects. Fix this.
- add bottom bar navigation
- move features out to different module from main application
- move feature logic out out to lib which doesn't have android dependencies
- -Xjspecify-annotations=strict, -Xtype-enhancement-improvements-strict-mod
- replace most if not all of the data sources with a Room based impl. This should allow for i18n, customisation, persistence of settings, editing, etc.
- add dictionary in addition to thesaurus
- update thesaurus data file. Add setting for which one (or both?)
- add notification group for stopwatches/timers, etc.
- new introspection app
