#!/usr/bin/env zsh

up() {
  v="$1"
  cd $(git rev-parse --show-toplevel)
  ./gradlew wrapper --gradle-version="$v" --distribution-type=all
  git commit -anm "[build] bump gradle to $v"
}

if [[ $# -ne 1 ]]
then
  printf "Usage: $0 version\n" >&2
  exit 1
fi

up "$@"
